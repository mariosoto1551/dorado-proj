# Progreso de ejecución — Proyecto Dorado

> Esta carpeta es el registro de lo que **realmente se hizo**, sesión a sesión de Claude Code — a diferencia de `docs/phases/`, que es la especificación (no cambia una vez escrita). Un archivo por fase, mismo nombre que su spec en `docs/phases/`, en `docs/progreso/`.

## Protocolo (leer esto antes de tocar código en cualquier sesión nueva)

### Al empezar una sesión

1. Leer esta tabla para saber qué fase está en curso o cuál sigue.
2. Abrir `docs/progreso/fase-XX-*.md` de la última fase marcada `COMPLETADA` (o `COMPLETADA_CON_DESVIACIONES`) y leer su "Registro de ejecución" completo — ahí están las desviaciones del plan original y qué hay que verificar antes de confiar en que esa fase sigue funcionando.
3. **No asumir que el código de una fase anterior anda solo porque el archivo dice `COMPLETADA`.** Correr los comandos de verificación que esa fase dejó anotados (tests, `docker compose up`, etc.) antes de construir encima.
4. Si la fase que vas a ejecutar ya tiene un archivo en `docs/progreso/` con estado `EN_PROGRESO`, retomar desde ahí — no volver a empezar de cero ni asumir que no se hizo nada.

### Al terminar una fase (o un corte de trabajo significativo dentro de una fase larga)

1. Completar (o actualizar) `docs/progreso/fase-XX-*.md` con el template de abajo — no dejarlo a medio llenar "para después".
2. Actualizar la fila correspondiente en la tabla de este README.
3. Un commit de git por fase (o por corte), con mensaje `fase-XX: <resumen corto>` — el historial de git es parte de este mismo sistema de trazabilidad, no algo aparte.
4. Si te desviaste de lo que dice `docs/phases/fase-XX-*.md` (agregaste un campo, cambiaste un endpoint, etc.), **documentar la desviación acá, no editar el archivo de spec original** — el spec queda como quedó decidido; el registro de ejecución es donde se anota la realidad.

## Tabla de estado

| Fase | Estado | Última actualización | Resumen |
|---|---|---|---|
| Fase 0 — Especificación | COMPLETADA | 2026-07-14 | Docs de arquitectura y las 15 fases generados. |
| Fase 1 — Monorepo | COMPLETADA_CON_DESVIACIONES | 2026-07-14 | Workspace Nx 23 + 12 apps + 5 libs + Docker + CI. Ver desviaciones (Nx 23, plantilla angular, zone.js devDep, shared-logging) y pendiente de CI en su archivo. |
| Fase 2 — Identity & Access | COMPLETADA_CON_DESVIACIONES | 2026-07-15 | identity-service completo (auth, invitaciones, grupos, internos, 4 eventos) + shared-auth/shared-logging reales. Flujo E2E y eventos verificados. Ver desviaciones (Prisma 7 config/extension, huecos de spec señalados) en su archivo. |
| Fase 3 — Gateway + auth frontend | COMPLETADA_CON_DESVIACIONES | 2026-07-15 | Gateway completo (proxy 9 prefijos, CORS, 429, JWT, headers tenant, health) + auth de app-web (sesión en memoria, refresh silencioso, 5 pantallas). Flujo E2E verificado en navegador. Ver desviaciones (express-rate-limit, pathFilter, tipos locales) en su archivo. |
| Fase 4 — Billing | COMPLETADA_CON_DESVIACIONES | 2026-07-16 | billing-service completo (suscripción FREE vía evento, entitlements, mi-organizacion) + límites reales en identity (grupos/tutores/usuarios) + plan real en el JWT con fallback FREE. 4 criterios verificados E2E. Ver desviaciones (seed dual, extras en sobre de error, fail-open de entitlements) en su archivo. |
| Fase 5 — Activity Catalog | COMPLETADA_CON_DESVIACIONES | 2026-07-16 | activity-service completo (CRUD actividades/conductas, límite de plan real, visibilidad por rol, validación condicional de tiempo). Incluye fix transversal de seguridad: el filtro automático de tenant (ALS) estaba inactivo desde Fase 2 — ver su archivo. |
| Fase 6 — Session/Section | COMPLETADA_CON_DESVIACIONES | 2026-07-16 | session-service completo (config por grupo, máquina de estados Sección/Sesión, scheduler automático con cron+timezone, 5 eventos, endpoints internos para Fase 7). Ambos modos verificados E2E. Ver desviaciones (mapeo DTO/columnas, autocierrePospuestoHasta, bootstrap automático) en su archivo. |
| Fase 7 — Scoring Engine | COMPLETADA_CON_DESVIACIONES | 2026-07-18 | scoring-service completo (ledger EventoPuntos, 2 colas cuórum consumidoras, evaluación por sesión/final, umbrales, descalificaciones, correcciones, internos para Fase 8) + endpoints de registro en activity-service (completar/no-hizo/conductas/cronómetro, 4 eventos). 6 criterios verificados E2E (43/43 checks). Ver desviaciones (origenId=registroId, ORG_ADMIN en completar, huecos de spec señalados) en su archivo. |
| Fase 8 — Rewards | COMPLETADA_CON_DESVIACIONES | 2026-07-18 | rewards-service completo (CRUD recompensas con snapshot de zona, elegibles derivados del ResultadoSeccion de scoring, selección/sorteo con canje único por sección, entrega, consumidor de ZonaAlcanzada). 4 criterios verificados E2E (29/29 checks). Ver desviaciones (shape de elegibles, codes de estado, ORG_ADMIN en canjes) en su archivo. |
| Fase 9 — Notification & Audit | COMPLETADA_CON_DESVIACIONES | 2026-07-18 | notification-service (campana in-app, 9 eventos notificables con plantillas y nombres resueltos por REST) + audit-service (ledger inmutable de solo lectura, timeline por entidad) + retrofit de AccionAdministrativaRegistrada en identity/activity/scoring/rewards. 5 criterios verificados E2E (18/18 checks). Ver desviaciones (shape de paginación, interno de tutores, fallback de nombres, SYSTEM en ciclo de vida) en su archivo. |
| Fase 10 — Frontend completo | COMPLETADA_CON_DESVIACIONES | 2026-07-21 | app-web completo (diseño híbrido tutor/usuario, shared-ui con ZonaBadge/EstadoSeccionBadge/ConfirmDialog, 6 clientes API, shell con nav por rol + campana con polling, 11 pantallas tutor + 4 usuario). Ciclo E2E completo verificado en navegador (17 pasos, tutor+usuario, scoring real). Ver desviaciones (fix CORS PUT en gateway, tipos de retorno de mutaciones de sección, huecos de API de estado propio) en su archivo. |
| Fase 11 — Public site | COMPLETADA_CON_DESVIACIONES | 2026-07-21 | public-site en Astro 7 (SSG + Tailwind 4 con tokens compartidos): landing premium (hero con mock de producto, showcase de zonas, bento de features, cómo funciona, CTA), /precios estático (Free vs Pro sincronizado con seed de billing) y /registro con island de TS vanilla contra el Gateway. Registro verificado E2E real (201/409 + submit desde navegador en origen 4321). Ver desviaciones (sin componentes de sección separados, dark mode solo por SO, progressive enhancement de reveals, scripts inline) en su archivo. |
| Fase 12 — QA/hardening | COMPLETADA_CON_DESVIACIONES | 2026-07-21 | Suite E2E (`apps/e2e`, Playwright, API-first) con 4 áreas: flujo completo con números conocidos (27→Verde), aislamiento entre tenants, inmutabilidad del ledger + secciones cerradas, carga (500 eventos) + DLQ. Orquestador `scripts/e2e-up.mjs` + 12 READMEs de despliegue. **10/10 verde** en corrida real contra el stack. Ver desviaciones (CI e integración dockerizada diferidos a Fase 13 por decisión de José, bypass de rate limit en registro/carga, DLQ forzada con veneno determinista) en su archivo. |
| Fase 13 — Piloto/deploy | COMPLETADA_CON_DESVIACIONES · **ESTABLE** | 2026-07-26 | Infra-as-code **deploy-ready**: Dockerfile parametrizado + docker-compose.stack.yml, `infra/render.yaml` (Blueprint 9 servicios + Postgres), vercel.json de ambos frontends (+ environment.prod.ts/fileReplacements verificado), `.env.production.example`, `docs/runbook-deploy.md` y job **e2e en CI** (cierra deuda de Fase 12). Plataformas: Render + Vercel + CloudAMQP. **José la confirmó ESTABLE (2026-07-24, ratificado el 2026-07-26)** → Fase 14 desbloqueada de forma definitiva (no se re-pregunta por ítem). Deuda residual (no bloqueante): carga de datos reales del tenant y corrida del deploy con sus cuentas quedan como operación, no como código pendiente. Ver archivo. |
| Fase 14 — Post-MVP | EN_PROGRESO | 2026-08-06 | **#29 asistente de IA — COMPLETO, 7 de 7 tandas (2026-08-04)**: primer ítem que suma un **servicio nuevo** al monorepo desde la Fase 9 (`ai-service`, puerto **3009**, prefijo `/api/ai`, base `ai_db`) y el primero que integra un **proveedor externo de pago**. Pedido de José con una pregunta técnica adentro —*¿projects o users de OpenAI?*—: **projects**, uno por entorno con su límite de gasto y un **service account** adentro; nunca una key de usuario (muere con la persona) y **nunca un project por tenant** (no escala y el aislamiento hay que hacerlo en el JWT igual). Cinco decisiones cerradas con él antes de escribir una línea: los tokens **los paga la plataforma** con cuota mensual por organización (BYOK evaluado y descartado), la IA **propone y el humano aplica**, **solo ORG_ADMIN y TUTOR** (lo que deja el ítem #4 afuera por construcción, ningún menor le habla a un LLM), cuatro capacidades, y **viene apagada** con opt-in del dueño y consentimiento registrado. La decisión estructural que gobierna todo: **la IA no tiene manos** — `ai-service` no conoce ningún secreto que le permita mutar otra base (sus clientes internos son todos `GET`) y **aplicar lo hace el frontend con el JWT del Tutor contra los endpoints que ya existen**, así que el peor caso de un prompt injection exitoso es una propuesta fea que un humano ve antes de aplicar; la segunda defensa es de la misma clase: **el tenant nunca es un argumento de una herramienta**. **Es el primer ítem de la fase que no revisa ninguna decisión anterior**, y no por casualidad: uno que necesitara cambiar una regla de negocio para que la IA funcione estaría construyendo la IA en el lugar equivocado. **T1**: `Plan.asistenteIa` + `cuotaTokensIaMensual` y los dos campos en `EntitlementsDto`, migración aditiva pura (FREE `f/0`, PRO `t/2000000`, verificado contra Postgres real). Dejó anotada una trampa que se cobró sola media hora después: una columna nullable nueva deja las filas viejas en `NULL`, que acá significa **sin límite**, o sea lo contrario de lo seguro — el gate es `asistenteIa` y la cuota no se lee sin pasar por ahí. **T2**: andamio completo con los 4 modelos, `comun/excepciones.ts` **desde el día uno** (rewards descubrió por E2E en el #26 que ningún `code` llegaba al cliente, con el servicio entero ya escrito del modo equivocado), el opt-in `GET`/`PUT /ai/configuracion` con el `PUT` restringido a `ORG_ADMIN`, y un `BillingClientService` **fail-CLOSED** al revés que el de activity y a propósito: allá el costo de un falso negativo es una actividad de más, acá es gastar dinero real contra un tercero. **El consumo se deriva sumando el ledger `Mensaje`, sin contador mutable** (regla 1 aplicada a los tokens). **Dos bugs los encontró la verificación, no la escritura**: el `?? 0` que colapsaba «sin límite» a «sin cuota» (lo agarró la unidad) y, el importante, **el servicio no arrancaba con la API key vacía** —`@IsOptional()` no saltea el string vacío y `.env.example` declara `OPENAI_API_KEY=`, así que copiar el ejemplo tal cual dejaba un servicio que no levanta, lo contrario del criterio entero de la tanda—: **tests, lint y build estaban los tres verdes** y lo destapó levantar el proceso de verdad, **quinto caso del mismo modo de falla en la fase** (tras `turnos-de-hoy`, el «✓ hizo», la vigencia y el consumidor round-robin). 18/18 ai-service (nuevos) + 9/9 billing + 36/36 gateway y el resto del workspace intacto, **lint 19/19 y build 18/18**, dos migraciones contra Postgres real **sin drift**, y arranque real verificado (rutas mapeadas sin API key, 401 sin JWT). Desviaciones: `EventoProcesado` **no se creó** (habría nacido muerta: sin RabbitMQ), `Mensaje` fuera de `MODELOS_TENANT` a propósito, y el generador `@nx/nest` **no acepta `--unitTestRunner=vitest`** (se generó con `none` y se copiaron a mano los configs de rewards). **T3**: los clientes internos y las 8 herramientas de lectura, con las dos defensas estructurales convertidas en **tests sobre la forma y no sobre el comportamiento** — uno recorre las definiciones y falla si alguna declara un parámetro de tenant (no prueba que hoy se ignore: prueba que **no hay dónde escribirlo**), el otro lee `src/clientes/` **como texto** y falla ante cualquier `method:` que no sea `GET`, ante cualquier `POST|PUT|PATCH|DELETE` y ante un `fetch(` fuera de la base. Lo que los hace verdaderos es la forma: todos los clientes extienden un `ClienteInternoBase` que expone **un solo método de red**, y `ContextoHerramienta` **solo lo sabe construir `AccesoGrupoService`**, así que no se puede leer un grupo que nadie validó. Ese acceso **no es copia del de los otros servicios y la diferencia importa**: allá un `grupoId` ajeno lo vuelve inofensivo el filtro de Prisma, pero `ai-service` no tiene tablas propias con estos datos —los pide por REST con el `grupoId` como parámetro—, así que para un `ORG_ADMIN` (que viaja con `grupoIds` vacío) si acá no se valida **no valida nadie**; verificado contra identity real con dos organizaciones. Endpoints internos nuevos en activity (3), scoring (1) y **rewards, que estrenó `InternalController`** (la spec de Fase 8 no le había definido ninguno). `resumen-puntajes` resuelve la Sección **desde el propio ledger de scoring** en vez de encadenar session-service, y devuelve si el número es definitivo o provisorio porque **el modelo tiene que poder decir «provisorio» cuando lo es**; `resumen-cumplimiento` cuenta solo marcas vigentes y **devuelve también las actividades en cero**, que son el caso que la herramienta existe para encontrar (en el grupo piloto apareció una). `listar_participantes` junta gente+roles+equipos en **una sola herramienta**: separarlas costaría tres vueltas del loop, o sea tres llamadas al proveedor pagadas para responder una cosa. **Sin emails ni `username`**, y no por un filtro: `UsuarioDto` no tiene email, la única forma en que esa regla sobrevive a las próximas cuatro tandas. **Dos bugs, ninguno de la escritura**: `ai-service` **nunca tuvo `internal-health.controller.ts`** —faltaba desde la T2 y el Gateway reportaba `ai: "down"` con el servicio arriba y contestando, lo que además volvía **imposible de verificar el criterio 9** («con ai-service apagado, el health lo reporta caído»: lo reportaba caído siempre), **sexto caso del mismo modo de falla en la fase**—, y un tipo inexistente (`EstadoCatalogo`) que **pasó tests y lint en verde** y solo agarró el `build`, recordatorio de que los tres targets miran cosas distintas. 47/47 ai-service (+29) con activity 357, scoring 63 y rewards 206 intactos, **lint 19/19 y build 18/18**, sin migraciones (no tocó ningún schema), **stack real levantado con los 8 endpoints internos en 200** y **las 8 herramientas ejecutadas contra los servicios reales**, más el **Gateway ruteando `/api/ai`** (pendiente #1 de la sesión anterior, cerrado). Fuera del código: `infra/render.yaml` sumó el **décimo servicio** y `.env.production.example` la sección de `ai-service`, que no existían. **T4**: el loop, primera tanda que **gasta dinero real**. José cargó la key y preguntó qué modelo usar —la spec no lo ancló a propósito—: **`gpt-5.6-terra`**, verificado contra la doc y probado con la key contra los tres vigentes. El criterio no fue «el del medio»: la tarea **no necesita razonamiento de frontera** (llenar veintipico de campos con el schema a la vista), pero **tampoco es barata**, porque una propuesta que no valida vuelve al modelo y un modelo flojo **gasta los tokens dos veces**. Se llamó a la API **antes** de escribir el loop, y trajo dos cosas que hubieran salido mal: **un turno devuelve VARIAS `function_call`** (asumir una por vuelta deja un `call_id` sin responder, que es como se cuelga un loop — por eso el tope de 8 cuenta **turnos, no llamadas**), y **los tokens de razonamiento se facturan y compiten contra `max_output_tokens`** (uno chico produce `incomplete` con texto vacío, que en pantalla se ve como que el asistente no contestó). **EL BUG DE LA TANDA lo encontró llamar de verdad**: la fórmula del `prompt_cache_key` que fija la spec —`org:<uuid>:grupo:<uuid>`— mide **83 caracteres** y el proveedor rechaza ese parámetro por encima de **64** (medido: 64 pasa, 65 no), así que **toda conversación terminaba en 503** y el síntoma era un `PROVEEDOR_NO_DISPONIBLE` genérico; resuelto con un sha-256 hex que mide exactamente 64, lo que además arregla **una contradicción interna de la spec** (su Parte E dice que el id de organización no sale en claro, y esa fórmula lo mandaba en claro). **Séptimo caso del mismo modo de falla en la fase.** Decisiones propias: **el historial NO reproduce las llamadas a herramientas** —el ledger guarda un resumen, no los datos, y que el modelo vuelva a leer es lo correcto: dos horas después la foto vieja miente—, las filas de herramienta van con **0 tokens** (lo que cuestan es entrar como entrada del turno siguiente, y eso ya se cuenta allá), un `ErrorConConsumo` que arrastra el parcial para que **un fallo en la vuelta 3 no borre la contabilidad de las vueltas 1 y 2**, y la tarifa de un modelo desconocido es **la más cara conocida y no cero** (sobreestimar es el error barato). 82/82 ai-service (+35), lint 19/19 y build 18/18, **sin migraciones**, y **conversación real por el Gateway con 12/12 checks** cubriendo los criterios 1, 2, 3, 4, 5 y 10: el modelo llamó dos herramientas en un turno y contestó con el catálogo real, la cuota agotada dio 402 **sin llamar al proveedor**, y un Tutor de otra org habilitada recibió 404. **Costo medido**: una conversación de 2 mensajes = 7.910 tokens = **USD 0,0117**, y sin caché habrían sido USD 0,0188 → **el `prompt_cache_key` ahorra 38%**. Al ritmo medido, una organización que queme los 2M del plan PRO cuesta **≈USD 3**, o sea que el tope de USD 5 del project de dev **está por debajo de dos organizaciones a cuota llena**. **T5**: las cuatro `proponer_*`, la validación Zod y el ciclo de vida de `Propuesta` — la tanda donde la asimetría del ítem se vuelve código (el modelo «llama» y no ejecuta nada; el servicio valida, guarda una fila y contesta «mostrala»). Para que **un cambio de DTO rompa el build acá** —lo que pide la spec— hubo que llevar los contratos de request a `shared-types` y hacer que las clases con decoradores los `implements`: no era posible antes, vivían sueltos en cada servicio. Dos cosas costaron: **los enums no son compatibles entre servicios** (activity valida contra los que genera Prisma y TypeScript los trata como tipos distintos aunque tengan los mismos valores; se resolvió con el tipo plantilla del enum) y **`z.ZodType<Contrato>` sola no alcanza**, porque por tipado estructural a un esquema le puede faltar un campo **opcional** y seguir siendo asignable —renombrar `siempreVisible` no habría roto nada y la propuesta habría dejado de poder configurarlo, un deterioro silencioso peor que un build roto—; se cerró con un chequeo de cobertura de claves **verificado a mano renombrando un campo a propósito**. **TRES bugs, los tres de llamar de verdad.** (1) La primera corrida armó 4 actividades de shape perfecto y **las 4 dieron 400 al aplicar**: faltaban los invariantes cruzados, o sea exactamente lo que la decisión 11 existe para evitar. (2) Al agregarlos como rechazos, dos corridas terminaron con el modelo **quemando las 8 iteraciones del loop** y **sin ninguna propuesta**: resulta que **el modelo no puede OMITIR una propiedad declarada** —emite todas, siempre— así que pedirle que omitiera era pedirle lo imposible. Eso cambió la regla del diseño: **se rechaza lo ambiguo, se normaliza lo determinado** (con `SIN_LIMITE` los campos van en null y no hay otra lectura: derivarlo el servidor es más barato que hacérselo adivinar). **Se mide**: el mismo pedido pasó de 49,5 s y 67k tokens sin propuesta a **19,6 s y 48k con la propuesta armada**. Corolario: en un alta `null`/`""`/`[]` son «no lo puse»; en un PATCH `null` **borra**, dos significados opuestos del mismo valor. (3) **El Gateway cortaba a los 30 s** con un 502 mientras ai-service seguía trabajando **y gastando tokens** —el Tutor veía error y la plataforma pagaba igual—: el `proxyTimeout` era fijo para todas las rutas y pasó a ser **por ruta**, con el default de la spec intacto y solo `/api/ai` en 120 s. Desviación registrada: **`proponer_precios_tienda` apunta a `/rewards/productos/:id` y no a `/recompensas/:id`** como dice la Parte D, porque **en `Recompensa` no hay ningún precio**. 126/126 ai-service (+44) + 41/41 gateway (+5, `tabla-ruteo.spec.ts` no existía), lint 19/19 y build 18/18, sin migraciones, y **ciclo completo contra OpenAI real por el Gateway con 14/14 checks** (criterios 6, 7 y 8): el modelo armó una propuesta de 4 actividades calibrada contra las zonas, **nada se escribió hasta aplicar**, y **las 4 operaciones se aplicaron tal cual con el JWT del Tutor** — la prueba real de que «aplicar es un `for`». Zod 4.4.3 es dependencia nueva. **T6 — el frontend y el SSE**: pantalla `/asistente` en el grupo Ajustes, chat con rastro de progreso, tarjeta de propuesta con el valor viejo al lado del nuevo, «Aplicar todo»/«Aplicar seleccionadas»/«Descartar», switch del `ORG_ADMIN` en el panel de organización y las dos entradas de contexto (catálogo vacío y rendimientos del #28). José eligió el streaming **más barato** —SSE de progreso con el texto entero en un evento— y la decisión no le quita nada: el proveedor cobra lo mismo con `stream: true` o sin él, y **de un turno de 4,6 s el texto tardó 2,1 s y el resto se fue en herramientas**, así que lo que hace usable la pantalla es saber *qué está haciendo*, no ver la última oración escribirse. Los dos endpoints que corren el loop **negocian por `Accept`** (navegador ⇄ scripts y E2E), y la regla del controller es que **el canal no se abre hasta el primer evento**: lo que rebota antes de gastar un token sale como 402/403/404 de verdad, no como un `200` con la mala noticia adentro. **EL CABLE RIESGOSO, MEDIDO**: el proxy **no bufferea** — primer evento a los **34 ms** y último a los **4638 ms** de un turno de 4639 ms, `text/event-stream` conservado y sin `content-length`. Conversación real en el **navegador** contra OpenAI: leyó las zonas, propuso 3 actividades, **el catálogo seguía en 0 con la propuesta a la vista**, y al aplicar quedaron creadas con el JWT del Tutor. Mirar la pantalla encontró dos cosas que ningún test iba a dar: la tarjeta se dibujaba **arriba** del rastro que la produjo (orden invertido respecto de lo que ocurrió) y el consumo decía «0%» después de gastar dinero (ahora «menos del 1%»). Y un test destapó un modo de falla que sí importa: `standardHeaders: 'draft-8'` llama a `response.append()` —de **Express**, no de `node:http`—, la librería capturó el fallo y lo pasó a `next(err)`, o sea que **el rate limit se apagaba en silencio** (11 de 11 pasaron). Rate limit por usuario del punto 5c hecho: **10 turnos/min por persona**, como paso 5 de `main.ts` porque su clave es el `sub` del token y antes del JWT ese dato no existe. 148/148 ai-service (+22) + 49/49 gateway (+8) + 205/205 app-web (+46), lint 19/19 y build 18/18 verdes, sin migraciones, y **suite E2E completa 71 pasadas con `E2E_UI=1`**: los dos fallos restantes son `public-site` no servido y un fallo **ajeno y nocturno de `destinatario-y-vigencia.e2e.ts`** (ítem #24) que **no se terminó de diagnosticar**: el test excluye el día de hoy según el reloj del runner y el servidor decide contra el día en que arrancó la Sesión en la timezone del Grupo. La hipótesis del desfasaje de zonas (UTC-4 vs UTC-3) **se descartó** —a las 00:00 locales las dos coinciden y sigue fallando—; queda como deuda del #24, a confirmar antes de escribir el arreglo. **T7 — la suite E2E con el proveedor stubbeado**: `OPENAI_BASE_URL` (nueva, opcional, default la URL real) apunta `ai-service` a un servidor local que habla **la Responses API de verdad** — `function_call`, `usage`, `call_id` por llamada—, así que **el cliente HTTP, el parseo y la contabilidad que se testean son los que corren en producción**; la alternativa (`if (esTest)` adentro de `OpenAiService`) haría que la suite verifique otro código, justo en el archivo donde eso más importa. `e2e-up.mjs` suma `ai-service` (novena base, décimo proceso) y **le pisa la key con una de mentira** además de apuntar la base al stub: si quedara la real y alguien cambiara la URL sin querer, cada corrida gastaría plata. **17/17 en dos corridas seguidas y en ~10 s cada una.** Los escenarios que más valen: con la cuota agotada el assert es `stub.llamadas === 0` (el pre-flight corta **antes** de gastar); un fallo del proveedor en el segundo turno deja **los 500 tokens del primero en el ledger** (Parte E 6 escrita como test: contabilizar solo lo que termina bien deja consumir gratis cortando la conexión); y el aislamiento se verifica **inspeccionando lo que salió hacia el proveedor** —aparece la actividad de ALFA, no la de BETA, y no hay ni un `@`—. **Criterio 9 verificado con `ai-service` realmente abajo**: el health lo reporta `down` y **las 17 pruebas de navegador del área Tutor pasan igual**. Dos expectativas mal en el test y no en el código: el stream devuelve **201 como el camino sin stream** (negociar por `Accept` cambia cómo llega la respuesta, no qué pasó) y el grupo ajeno da **404 y no 403** (no se confirma que exista). **Los 12 criterios de aceptación de la spec quedan cubiertos.** Pendientes anotados: **los invariantes son un espejo de activity y pueden derivar**, y `scripts/e2e-up.mjs` **sigue sin levantar `ai-service`**, como decisión y no olvido: el criterio 9 pide correr la suite con el servicio abajo. Ítems anteriores: se ejecuta ítem por ítem (ver `fase-14-post-mvp.md`). Hechos: panel PLATFORM_ADMIN (#5), roles/multi-grupo, multi-grupo con misma cuenta, confirmación de obligatorias (#8), equipos de trabajo (#9, E2E 23/23). En curso: **contenido creado por los integrantes (#10)**, **actividades programadas por día (#11)**, **marcas rojas del tutor (#12)**, **anular tareas de equipo (#13)**, **prioridad visual de la lista del integrante (#14)** y **tareas de equipo visibles pero no marcables en la lista (#15)** —los seis agregados/ejecutados el 2026-07-26—, backend+frontend completos y verificados en tests/build/lint (157/157 activity, 57/57 scoring, 22/22 notification, 17/17 app-web; lint de 18 proyectos y build de 17 verdes), falta aplicar las 4 migraciones nuevas contra DB real + E2E. **#16 scheduler con recuperación (2026-07-27)**: el scheduler de Fase 6 disparaba por igualdad de minuto y **perdía toda transición que cayera con el proceso caído** (un deploy bastaba); ahora reconcilia la ventana `(evaluadoHasta, ahora]`. 74/74 session verde, migración aplicada en local y **verificado contra Postgres real** — esa verificación destapó un `$queryRaw` sobre `pg_advisory_xact_lock` que pasaba tests/lint/typecheck/build y fallaba en el 100% de las corridas reales. **#17 plan del día (2026-07-27)**: la lista del integrante mostraba **todo el catálogo, todos los días** (20 opcionales = 20 tarjetas diarias, con las obligatorias ahogadas entre opciones); ahora las OPCIONALES individuales del catálogo del tutor se ocultan hasta que el integrante las elige desde una hoja «＋ Elegir», y el plan dura un día. **Se activa por Grupo y viene apagado**, así que ningún grupo existente cambia de comportamiento. 185/185 activity (28 nuevos) + 27/27 app-web (10 nuevos), migración aplicada en local, **verificado contra Postgres real** y **E2E 14/14 verde** (suite nueva `plan-del-dia.e2e.ts`, 4 tests, corrida dos veces). Deuda anotada a pedido de José: tope configurable de tareas por día. El ítem #7 quedó **cerrado por absorción** en el #10. Nota: `admin-web:test` falla porque ese proyecto no tiene ningún `.spec.ts` (deuda declarada del #5), no por una regresión. **Alcance nuevo (2026-07-30)**: se sumaron al índice los ítems **#18 historial de la sesión**, **#19 roles del participante en el Grupo**, **#20 las obligatorias también suman** (revisa la decisión 2 del #8) y **#21 turnos rotativos**, con sus decisiones de alcance ya cerradas con José en esa sesión — **sin spec ejecutable ni código todavía**. Orden de ejecución sugerido y justificado: **18 → 20 → 19 → 21** (ver la sección al pie de `fase-14-post-mvp.md`). **#18 historial de la sesión EJECUTADO (2026-07-30)**: línea de tiempo del grupo como pestaña del panel operativo, armada uniendo las tres tablas de registro sin materializar nada, con anular/deshacer/conducta rápida/notas internas. 208/208 activity (23 nuevos) + 34/34 identity + 34/34 app-web (7 nuevos), lint y build verdes, migración aplicada contra Postgres real y **suite E2E nueva 4/4 verde dos veces**. Desviación: hizo falta un interno nuevo (`GET /internal/identity/grupos/:id/equipos`) que la spec no preveía, para no romper el invariante de llamadas constantes por request. **#20 obligatorias que suman EJECUTADO (2026-07-30)**: `Actividad.puntosPorCumplir` (+2 si la cumple / −10 si no), acreditado al confirmar; revisa la decisión 2 del #8 sin editar su spec. 217/217 activity (9 nuevos) + 34/34 app-web, lint y build verdes, migración aplicada contra Postgres real y **E2E nueva 3/3 verde dos veces** (7/7 junto con la del #18). Hallazgo anotado, sin cambiar comportamiento: tras «confirmar → no hizo → deshacer» el integrante no puede re-confirmar (el intento se quemó, regla del #12) y el neto queda en 0 — decisión de producto pendiente para José. **#19 roles del participante EJECUTADO (2026-07-31)**: `RolGrupo` por Grupo en identity (nombre + color, archivable) y `Actividad.rolesPermitidos` en activity; una actividad restringida **se oculta** para quien no tiene el rol. La spec (`fase-14-19-roles-del-participante.md`) se escribió en esa misma sesión —el ítem solo tenía decisiones de alcance— cerrando tres huecos con José. Desviación deliberada del índice: la asignación quedó como **campo `UsuarioGrupo.rolGrupoId`, no como tabla `UsuarioRolGrupo`** (con un rol por participante, el invariante lo garantiza el esquema en vez de la lógica de aplicación). El filtro se aplica en **cinco** caminos, y el que importa es el quinto: **el castigo automático al cerrar la Sesión**, que no se ve en ninguna pantalla y que sin él restaría puntos a quien nunca vio la actividad. **Costo cero cuando el grupo no usa roles**: el cruce REST solo se paga si el catálogo tiene alguna restricción (mismo gate que `necesitaTimezone`), con tres tests que lo verifican con espías. 244/244 activity (27 nuevos) + 48/48 identity (14 nuevos) + 39/39 app-web (5 nuevos), lint y build verdes, ambas migraciones aplicadas contra Postgres real **y verificadas sin drift** (`migrate diff` → "No difference detected"), **E2E nueva 4/4** y suite completa **25/25 en dos corridas**. Sin eventos nuevos y sin tocar scoring/session/rewards/notification. **#21 turnos rotativos EJECUTADO (2026-07-31)**: cierra los cuatro ítems del 2026-07-30. José pidió en esa sesión que el patrón pudiera ser **`José - Luciana - José - Alejandra`** —alguien con más turnos que otro—, lo que **cambió el modelo del índice**: el turno dejó de ser «un pozo que rota uno por uno» y pasó a ser una **lista ORDENADA de posiciones** donde una persona puede aparecer varias veces (4 posiciones, 3 personas, José 2 de cada 4). La repetición vive en los datos, no en el algoritmo — salió más simple que la rotación pareja. Decisiones nuevas: secuencia literal (no pesos), el azar baraja **posiciones** y no personas (así conserva la proporción), y las altas/bajas del grupo **no editan la lista sola** (avisa y saltea). Implementación: la **vuelta se sella entera al empezarla**, así el azar es auditable y editar la secuencia no le cambia el día a nadie que ya lo tenía; el sellado consume `SesionAbierta` **sobre la cola que ya existía** (`activity.q.sesiones`, del #8); con turno activo la obligatoria **deja de ser de todos** y el castigo del cierre alcanza solo al asignado; rol (#19) y turno se combinan por intersección, y si ninguna posición queda válida **no se castiga a nadie**. 279/279 activity (35 nuevos) + 44/44 app-web (5 nuevos), lint y build verdes, migración aplicada contra Postgres real y sin drift, **E2E nueva 5/5** y suite completa **30/30 en dos corridas**. Sin eventos de dominio nuevos y **sin tocar la tabla `Actividad`** (retro-compatible por construcción). **#23 claridad del área del Tutor, tanda 1 (2026-08-01)**: primer ítem que **revisa lo ya construido** en vez de agregar capacidad, nacido de tres molestias que José reportó usando la app (no saber si algo quedó guardado, no encontrar dónde está cada cosa, pantallas sobrecargadas). Se organiza en 5 tandas —transversal antes que pantalla por pantalla— y su spec se escribe tanda por tanda. La **T1 (turnos)** encontró que el síntoma *«no se sabe si la actividad es por turnos o es uno cualquiera»* **no era de redacción**: la lista nunca mostraba la rotación, el endpoint que la resuelve (`turnos-de-hoy`, del #21) existía **y ninguna pantalla lo llamaba**, y el modal tenía **dos botones de guardar** —el principal no guardaba los turnos, y «Guardar turnos» + Cancelar los dejaba guardados igual—. Corregido con chip `🔁 Hoy: <nombre>` en la tarjeta, armador convertido en componente controlado y **un solo submit**; el armador además ya existe **al crear**, cerrando dos pendientes que el #21 había dejado anotados. **Cero backend** (279/279 de activity-service intactos), 58/58 app-web (14 nuevos), lint y build verdes, **primera suite E2E de navegador del repo** (`turnos-visibles.e2e.ts`, 3/3 en dos corridas) y suite completa **34/34**. **#23 tanda 2 (2026-08-02)**: extracción a `libs/shared-ui` de los patrones que estaban copiados a mano pantalla por pantalla. Al re-contar el inventario aparecieron dos cosas que el relevamiento preliminar no tenía: **el modal era la duplicación más cara** —15 copias, 11 con las clases del panel idénticas carácter por carácter, y **ninguna** declaraba `role="dialog"` ni cerraba con Escape— y **16 de los 64 campos no tenían anillo de foco** (WCAG 2.4.7), no por diseño sino por copiar la cadena equivocada. Forma **mixta** decidida con José: clase CSS para lo que es solo piel (`.tarjeta`, `.campo`, `.boton`+tonos, `.etiqueta-campo`, `.botonera`, apoyadas en que Tailwind 4 deja que las utilidades pisen a `components`, así `class="tarjeta p-0"` funciona y la excepción queda a la vista) y componente para lo que tiene estructura (`ui-modal`, `ui-estado-vacio`, `ui-campo`). Migradas las **16 pantallas del Tutor, las tres grandes incluidas**: 225 reemplazos de cadenas, 17 estados vacíos, 44 campos y 15 modales; el área Usuario queda afuera a propósito y no cambió ni un archivo. **La E2E encontró una regresión que ningún unit test iba a encontrar**: el contenido proyectado lo crea la página, así que el `@if` interno del modal **no lo destruye al cerrar** —el estado de un hijo sobrevivía y reaparecía al reabrir, que es justo la sensación que este ítem existe para eliminar—; corregido devolviendo el `@if` a la página adentro del `ui-modal`, y documentado en el componente porque no es evidente. 58/58 app-web + **18/18 shared-ui (los 18 nuevos: la librería no tenía ningún test de componente)**, lint y build verdes, y suite E2E completa **34/34 en dos corridas seguidas**. Cero backend. **#23 tanda 3 (2026-08-02)**: navegación. Se escribió primero una **herramienta de capturas** (`apps/e2e/src/capturas-tutor.e2e.ts`, gated por `E2E_CAPTURAS=1`, no verifica nada) que recorre las 16 pantallas del Tutor con un grupo cargado — las tandas 1 y 2 se diagnosticaron leyendo código, pero *dónde está cada cosa* no se ve leyendo archivos sueltos. Sirvió para cerrar el paseo visual que la T2 había dejado pendiente y para relevar esta tanda sobre hechos: apareció que **«Primeros pasos» estaba TRES veces en la misma pantalla** (menú, tarjeta del Resumen y píldora flotante, las tres al mismo destino) y que **el Resumen, sin Sección activa, era una sola tarjeta vacía** con dos botones del mismo peso. Tres cambios: **hub `/configuracion`** que reúne las seis cosas que definen el grupo —cuatro se editan ahí, Zonas y Roles siguen siendo pantalla propia y muestran su estado—, con los bloques agrupados por **la pregunta que responden y no por el servicio que los guarda**; **menú de cuatro grupos** (Día a día / Catálogo / Gente / Ajustes) subiendo Entregas y Reportes a lo diario y bajando Zonas a Ajustes; y **el Resumen convertido en home real** con jerarquía en vez de cuatro bloques en paralelo —una sola cosa grande, «te esperan» condicional, «Hoy» en tres líneas— porque José preguntó si no era demasiado y lo era si todos pesaban igual. `/configuracion-sesion` redirige para no romper enlaces, y las pantallas de catálogo conservan una línea de estado que linkea al hub: mudar un control sin dejar rastro convierte «no sé dónde está» en «desapareció». 70/70 app-web (12 nuevos) + 18/18 shared-ui, lint y build verdes, y **E2E 37/37 en dos corridas seguidas** (3 nuevos). Cero backend, la tercera tanda seguida. Deuda anotada: el home pasó a costar **nueve llamadas** y ya rozó el límite de 100 req/min del Gateway en la E2E. **#23 tanda 4, primera vuelta (2026-08-02)**: las dos pantallas más cargadas —Panel operativo y Actividades—, relevadas esta vez **con Sección abierta y registros hechos** (las pantallas operativas vacías no sirven para juzgar si están recargadas). El hallazgo principal: **el Tutor no podía marcar «completó» desde ninguna pantalla**, siendo la acción más frecuente del día — y no por decisión de producto: el endpoint acepta `usuarioId`, declara `@Roles(TUTOR)` **y audita quién marcó** desde el #8, y el cliente del frontend ya tenía la firma exacta. Es **el mismo modo de falla que la T1 encontró con `turnos-de-hoy`**: la función entera y el cable faltante, dos veces en cuatro tandas, las dos sobre capacidades construidas «para más adelante». La pestaña «Registrar» pasó de **tres formularios apilados con la misma forma** —y con la persona a re-elegir en cada uno— a **un flujo por persona**: se elige una vez, cada actividad se marca con un clic, y «Corregir completadas» dejó de ser un formulario aparte porque el contador y el «quitar» viven en la misma fila. El modal de actividades pasó de **13 campos y 4 bloques condicionales** a tres campos a la vista y tres secciones plegadas **que muestran su estado** (plegar no es esconder). **Primera tanda del ítem que toca el backend**, y por una razón acotada: para que el Tutor marque sobre la lista real del integrante hace falta *esa* lista, que solo existía como `@Roles(USUARIO)`; componerla en Angular obligaba a **reimplementar las reglas de visibilidad de cinco ítems** (#10, #11, #17, #19, #21) en la interfaz. `miEstadoHoy` se generalizó a `estadoHoyDe(tenant, grupoId, usuarioId)` con **cero cambio para el integrante** (hay un test que compara las dos salidas con `toEqual`), más un GET nuevo — **sin schema, sin migración, sin evento**. Dos huecos que encontró la verificación: «Quién la hace» **no se abría sola cuando la actividad solo tenía turnos** (el turno no está en `ActividadDto` y llega después; se resolvió con el mapa de `turnos-de-hoy` que la lista ya tenía cargado), y **las tareas de equipo llegaban con botón «✓ hizo» que el backend rechaza** con 400 — ahora se ven, dicen *«La marca el jefe del equipo»* y no ofrecen acción, que es leer el criterio «la misma lista que ve el integrante» hasta el final: las mismas restricciones, no solo los mismos ítems. **Desviación registrada**: las tres secciones del modal quedaron *Cómo se cumple / Cuándo se puede hacer / Quién la hace*, no las de la tabla de la spec (el límite de tiempo es una pregunta sobre *cuándo* y se absorbió ahí); siguen siendo tres, y la spec **no se edita**. 82/82 app-web (12 nuevos) + 283/283 activity-service (4 nuevos) + **24/24 shared-ui (6 nuevos: `ConfirmDialog` existía desde la Fase 10 sin ningún test, y se le escribió uno al sumarle el motivo opcional)**, lint y build verdes, **E2E nueva 5/5** y suite completa **42/42 en dos corridas seguidas**. Deuda de tooling saldada en el camino: `entrarComoTutor` estaba copiado en cuatro suites y **solo la de capturas tenía el reintento** del límite de 10/min de `/auth/login`; se extrajo a `support/navegador.ts` — el mismo diagnóstico de la T2 sobre las pantallas, aplicado a los tests. **#23 tanda 4, segunda vuelta (2026-08-02)**: Historial, Equipos y Recompensas, las tres que la primera vuelta había dejado para después. Hizo falta **volver a extender la herramienta de capturas** —Equipos y Recompensas salían vacías, y una lista vacía no dice nada sobre si la pantalla está recargada— y recién con datos apareció el hallazgo: **seis acciones destructivas se ejecutaban con un clic**, en la sesión siguiente a la que había establecido lo contrario para el panel operativo, y el motivo de anular en Equipos era **un campo permanente y único para toda la pantalla**, el mismo defecto recién corregido en la pantalla de al lado. Es el tercer caso del ítem del mismo modo de falla y el más incómodo, porque el autor de las dos partes es el mismo: **una decisión tomada para una pantalla no se propaga sola a las demás**. Regla que cerró José: **se confirma lo que no tiene vuelta atrás, no todo lo que es rojo** — anular una marca del historial **sigue siendo de un clic** porque tiene «Deshacer» al lado, y archivar un producto o una bolsa **sí** pregunta porque no hay endpoint de reactivación. Además: los dos modales de equipo («Sustituir jefe» + «Integrantes») fusionados en **«Quiénes están»**, con hacer jefe y quitar en la fila de cada persona; **«Registrar conducta rápida» dejó de estar dos veces** en la misma pantalla; y las pestañas de Recompensas pasaron al control segmentado del panel, que **sí declaraba `tablist`/`aria-selected`** mientras las otras no declaraban nada. **Dos correcciones al diagnóstico aparecieron al implementarlo**, las dos por haber inventariado con `grep`: anular una entrega **ya confirmaba** (el `(click)` llama a un método llamado `anular` que en realidad abre el diálogo), y archivar **no** era reversible como se había supuesto en la conversación. Se corrigió la spec antes de tocar código. **Deuda de E2E saldada**: el presupuesto de 100 req/min por IP pasó de molestia a bloqueo con la quinta suite de navegador, y se resolvió con un **seam de configuración** (`RATE_LIMIT_GLOBAL`/`RATE_LIMIT_AUTH`, definidas solo por `scripts/e2e-up.mjs`) que **deja intacto el default de la spec** y en el que un valor basura cae al límite estricto, no a «sin límite» — la corrida completa pasó de ~8 min con 4 fallos a **55 s con 0**. 82/82 app-web + 24/24 shared-ui + 36/36 gateway (3 nuevos) + 283/283 activity-service sin tocar su código, lint y build verdes, **E2E nueva 6/6** y suite completa **48/48 en dos corridas seguidas**. **#24 destinatario y vigencia EJECUTADO (2026-08-03)**: pedido de José tras usar la app — tres molestias que son la misma vista de tres lados (**la actividad no dice para quién ni hasta cuándo es, así que la pantalla tampoco puede decirlo**): no se podía asignar a personas puntuales, todo era diario o por día de la semana, y la lista del Tutor era un `@for` plano en orden de creación. Cuatro modos de destinatario **excluyentes** (todo el grupo / rol / personas / equipos), vigencia `desde`/`hasta` que **se cruza** con los días del #11, y lista agrupada con buscador. **Cobra el diseño del #11**: aquella spec declaró `comun/programacion.ts` punto único de extensión «para cuando se agreguen fechas concretas» y funcionó como estaba escrito — la vigencia entró entera por `estaDisponibleEn` y los **8 puntos de enforcement la heredaron sin lógica nueva**, enumerados por el compilador al cambiar la firma a un objeto de parámetros. Es el primer ítem que cobra una extensión que otra spec dejó preparada por nombre. Lo que sí costó: el destinatario es la **tercera** regla de «quién ve qué» (con autoría del #10 y rol del #19), y con tres sueltas «aplicar dos y olvidar la tercera» dejaba de ser improbable — se **compusieron las tres** en `comun/destinatario.ts`, sin cambiar el comportamiento de las anteriores. Decisiones: el modo **no se guarda**, se deriva de qué array está lleno (un enum no evitaría el estado inconsistente y sí obligaría a migrar toda fila existente), y la validación mira los valores **finales** porque en un PATCH parcial la ambigüedad nace del cruce request+fila; las fechas son `String` `"YYYY-MM-DD"` **civiles del calendario del Grupo**, como `deadlineHora` es `"HH:mm"` local (un `DateTime` obligaría a decidir a qué hora y en qué zona empieza el «1 de marzo», el error que el #11 se cuidó de no cometer); `vigenciaVencidaEn` es una función **aparte** de `estaDisponibleEn` — lo que hoy no toca vuelve mañana, lo vencido no vuelve, y mezclarlas habría archivado toda actividad de los martes cada miércoles—; el **archivado automático** vive en el consumidor de `SesionCerrada` del #8, el único punto que corre una vez por día por grupo con la fecha y la timezone ya resueltas (sin cron nuevo, y **fuera de la transacción del castigo**). **Costo cero para quien no usa el ítem**: `ContextoParticipanteService` pide a identity solo lo que el catálogo exige, y para el cierre arma el contexto de todo el grupo con **dos llamadas en total**, no dos por persona. Hallazgo del camino: un test del #11 llamado «envelope sin fechaInicio» **nunca probó lo que decía** — en JavaScript un `undefined` explícito dispara el valor por default del parámetro, así que el payload siempre viajaba con la fecha; pasaba por otro motivo. Corregido con centinela `null`. **El ocultamiento lo encontró la E2E, no la unidad**: la decisión 10 dice que fuera del rango la actividad **no aparece**, y lo implementado cubría el bloqueo del registro (409) pero **no el filtro de la lista** — la vencida seguía llegando con su botón y el clic terminaba en error. Misma clase de falla que la T4 del #23 con las tareas de equipo; ningún unit test lo agarraba porque todos verificaban que completar fallara, que es la mitad de la regla. La causa de raíz era de orden en el código (la timezone se resolvía **después** de armar la lista), y el filtro usa `motivoNoDisponible === 'FUERA_DE_VIGENCIA'` y no `estaDisponibleEn`: **la vigencia oculta, el día apaga**. 346/346 activity-service (63 nuevos) + 104/104 app-web (22 nuevos), lint y build verdes, **migración aplicada contra Postgres real y verificada**, y **suite E2E completa 38/38 en dos corridas seguidas** (8 nuevos). **#25 objetivo de ahorro y mínimo de repeticiones EJECUTADO (2026-08-03)**: dos pedidos de José en el mismo mensaje, y el segundo empezó como pregunta — *¿qué pasa si una obligatoria de hasta 3 repeticiones se hace 2 veces?*. La respuesta era **nada**: el castigo del cierre era binario por par (usuario, actividad), así que **una sola confirmación evitaba el descuento** y el máximo era techo del premio del #20, no exigencia. No estaba escrito en ninguna spec: era una consecuencia no enunciada del `Set` de pares del #8. Ahora `Actividad.repeticionesMinimasSesion` (default **1** = comportamiento previo exacto) define el umbral y el castigo es **proporcional** —`−valorPuntos × faltantes`, decidido con José frente a las dos tablas de resultados—, simétrico con el premio que ya escalaba por repetición; una sola fila `NO_HIZO` con el total, y **las repeticiones que el Tutor quemó bajan el mínimo** para no castigar dos veces el mismo hecho. Del otro lado, `ObjetivoParticipante` en rewards: el participante marca un producto de la tienda como objetivo, lo ve arriba de su billetera con barra grande, y **el Tutor lo ve en Billeteras** (media razón de ser del objetivo es que el adulto lo refuerce fuera de la app); comprarlo lo limpia, y es lo único de la economía que **no es ledger** —se pisa, no se versiona—. **Hallazgo del camino, de la misma familia que la T1 y la T4 del #23**: la pantalla del integrante tenía un tope propio que la spec no preveía (`topeAlcanzado` devolvía `confirmada`), así que el botón desaparecía tras la primera confirmación y **un mínimo de 3 habría sido inalcanzable desde la interfaz** con castigo automático garantizado; se detectó antes de que llegara a producción. 357/357 activity-service (11 nuevos) + 143/143 rewards-service (13 nuevos) + 117/117 app-web, lint y build verdes, **ambas migraciones aplicadas contra Postgres real**. **Segunda vuelta el mismo día** (José: «corregí todo lo que haya que corregir»): se corrió la **E2E completa que la sesión del #24 había dejado pendiente — 38/38 verdes, sin regresiones**, se sumó la suite propia del ítem (`minimo-de-repeticiones.e2e.ts`, 5 tests) que verifica el castigo proporcional **contra el ledger de scoring** —lo único que la unidad no puede probar: ella comprueba la fila que escribe el cierre, no que scoring reste lo que esa fila dice—, y se cerró un hueco de la misma familia que el del integrante: **el mínimo tampoco se veía en la lista del Tutor**, que es quien puede hacer algo al respecto durante el día («1 de 3 · faltan 2»). Quedan abiertos, anotados: la vuelta manual del objetivo en navegador y **la decisión de producto del `NO_HIZO` deshecho** (no castiga al cerrar y tampoco deja re-confirmar — los dos lados del mismo caso, uno anotado por el #20 y el otro por este ítem). **#26 etiquetas del catálogo EJECUTADO (2026-08-03)**: pedido de José con la solución adentro y una duda declarada («quizás seleccionar todos de x etiqueta»). El catálogo de ítems era **una lista plana sin ningún eje de organización** —un `@for` en orden de creación, sin filtro— y el costo real no era leerla sino **lo que hay que hacer de a uno porque no existe forma de nombrar un subconjunto**: armar una bolsa de «premios chicos» es tildar 12 sobre 40 y rehacerlo cada vez que se agrega uno, y en modo `TIENDA` cada premio necesita su producto cargado a mano. Cuatro decisiones cerradas con él: **catálogo por Grupo** (no texto libre, que a la semana produce «Juguetes»/«juguetes»/«juguete»), **varias por ítem** —a diferencia del rol del #19, porque allá la cardinalidad 1 evitaba conflictos entre roles y **el rol restringe**, mientras la etiqueta no hace nada por sí sola—, **solo la ve el Tutor**, y tres usos y ninguno más. José **delegó el tercer uso**; se eligió publicar productos en masa y se descartó asignar zona en masa por un motivo que no es de gusto: el select de zona está **deshabilitado al editar**, así que el lote habría permitido lo que la pantalla prohíbe de a uno. Decisiones propias: **archivar una etiqueta es reversible y por eso NO pregunta** —lo contrario de productos y bolsas, y por el mismo criterio del #23 T4 (*se confirma lo que no tiene vuelta atrás*): desarchivar un producto vuelve a poner algo **comprable**, desarchivar una etiqueta vuelve a mostrar un chip—; **archivar no desasigna** (opuesto al #19, donde la asignación ocultaba actividades); y la asignación va por **`PUT` sobre un sub-recurso**, porque en un PATCH parcial un array vacío es indistinguible de «no lo mandé», la misma ambigüedad que el #24 resolvió a mano. `Recompensa` **no cambia una sola columna**: dos tablas nuevas, migración aditiva pura, sin evento. **El compilador atajó un bug real** al sumarle el segundo parámetro al mapeador: los dos `.map(recompensaADto)` de `canjes.service.ts` habrían inyectado **el índice del array** como etiquetas, justo en el camino de los elegibles del participante — o sea el único lugar donde la decisión «solo la ve el Tutor» podía filtrarse. **Y la E2E encontró algo que ningún unit test iba a encontrar**: los `code` de negocio de `rewards` **no llegaban al cliente**. `HttpExceptionFilter` solo conserva el code cuando la excepción extiende `DomainException`, y todo el servicio usaba `new BadRequestException({ code })`, cuyo code el filtro descarta — los tests unitarios no lo ven porque miran la excepción cruda, donde el code sí está. Se creó `comun/excepciones.ts` (rewards era el único servicio con codes de negocio y sin ese archivo) y **queda anotada la deuda**: los siete codes del #22 siguen llegando como `VALIDACION`, sin bug visible hoy porque ninguna pantalla ramifica por code. **Segundo hallazgo, de entorno**: la primera corrida dio 40/9 con tres fallas del #25 que estaban verdes — los **nueve** puertos 3000-3008 estaban tomados por procesos `dist/` de las 16:23 y la suite entera corrió contra un binario viejo, sin ningún error visible al arrancar; después de matarlos bajó de 6,1 min a 57 s. La trampa estaba anotada solo para 3000-3002: **vale para los nueve**. 168/168 rewards-service (25 nuevos) + 129/129 app-web (9 nuevos), lint y build verdes, migración aplicada contra Postgres real, y **suite E2E completa 49/49 en dos corridas seguidas** (6 nuevos). **#27 termómetro de zonas EJECUTADO (2026-08-03)**: pedido de José para el home del grupo («mi mamá sí o sí quiere un termómetro») con **una duda técnica declarada** — si el hover alcanzaba para ver a participantes amontonados en el mismo punto. La respuesta no era sobre amontonamiento: **el hover no existe en un teléfono**, que es donde se mira esta pantalla, así que el diseño no podía depender de él en ningún caso. Quedó resuelto con **etiquetas que se separan solas y línea guía al punto real** —todos los nombres siempre escritos, sin hover y sin tocar nada—, dentro de un **selector de vistas** (`Lista` / `Termómetro`, una a la vez y recordada) para no volver a alargar el resumen que el #23 T3 acababa de acortar. Decisiones: la zona más alta, que no tiene `puntosMax`, se dibuja **con el alto de la anterior más una flecha** y no con techo dinámico (que deforma la escala cada vez que alguien suma puntos y la vuelve incomparable entre semanas); **escala lineal en puntos, no bandas de igual alto** (Verde mide 25 puntos y Rojo 10 — igualarlas se ve más prolijo y hace que la altura de una marca deje de significar su puntaje); y **el mercurio es el promedio del grupo**, no un participante, porque un termómetro muestra *una* temperatura. Pista de carrera, podio y evolución diaria se ofrecieron y se descartaron. **Es el primer ítem sin `fase-14-NN-*.md` propio**, y queda anotado como excepción con su criterio: es frontend puro —sin schema, endpoint, evento ni campo de DTO, porque `listarUmbrales` y `puntajesDeGrupo` ya traían todo—, y escribir la spec *después* habría sido un documento fechado como decisión previa que en realidad describe lo ya hecho. **En cuanto un ítem toque backend, vuelve la spec antes del código.** La geometría vive en `core/termometro.ts` y se testea sin montar Angular, como `core/turnos.ts` del #21 y `core/etiquetas-catalogo.ts` del #26. Primer uso de `localStorage` en `app-web` (preferencia de vista, no token — comentado en el código para que no se lea como violación de la regla 7). 149/149 app-web (20 nuevos), lint y build verdes; **sin backend tocado y sin verificación en navegador** — es lo primero que debería hacer la próxima sesión, junto con probarlo en un teléfono real y preguntarle a la mamá de José si es el termómetro que tenía en la cabeza. **#28 monedas por cumplir EJECUTADO (especificado 2026-08-03, ejecutado 2026-08-04)**: la segunda fuente de la economía. El #22 montó un ingreso **semanal y atado a la zona** —tender la cama diez veces y tenderla cuatro caían en la misma zona y rendían lo mismo—; ahora completar una actividad, confirmar una obligatoria, cerrar una tarea de equipo o registrar una conducta BUENA **paga al instante**, con puntos y monedas **independientes** (el pedido literal de José). `activity-service` **no cambió ni un campo de schema**: el valor vive en `RendimientoAccion` de rewards, decisión 11 del #22 aplicada igual, y la migración es aditiva pura. **El baseline se cumplió exactamente** tras el cambio más riesgoso —quitar los guards de `registro.service.ts` (del #20) y mudar el descarte del 0 a `ProyeccionService`—: 357/357 activity con los 4 tests que *afirmaban el guard viejo* reescritos, y el invariante *el contenido de `EventoPuntos` no cambia ni una fila* sostenido. **Un hueco que la spec no vio**: el descarte del 0 rompía también la COMPENSACIÓN — `compensarCadenas` lanza cuando no encuentra el asiento original, así que quitar una confirmación de 0 puntos (el caso exacto que el ítem habilita) iba a la DLQ. La ausencia de asiento es ambigua —«valía 0» vs «llegó desordenado», y la segunda tiene que seguir fallando ruidosa—, así que se resolvió con un `valorPuntosSnapshot` **opcional** en dos payloads: con `0` explícito no hay nada que compensar, sin el campo se compensa como siempre. Desviación registrada (D.1 decía «payload cero»), spec **no editada**. **EL BUG DEL ÍTEM lo encontró la E2E, no la unidad**: el consumidor declaraba **ocho `@RabbitSubscribe` sobre la misma cola**, y RabbitMQ reparte round-robin **entre consumidores, sin volver a mirar la routing key** — un `ActividadCompletada` caía en el handler de tareas de equipo y explotaba en `asignaciones.map`. Los 32 tests unitarios no podían verlo porque llamaban a cada método directamente: ahí el ruteo lo hacía el test. El patrón correcto **ya estaba en el repo** (`ScoringConsumer.onRegistro`: un handler, `routingKey: [...]`, `switch` por `eventType`); se reescribió así y ahora **todos** los tests entran por la única puerta real, más tres nuevos sobre el despacho en sí. Es el **cuarto caso del mismo modo de falla en Fase 14** —tras `turnos-de-hoy` (#23 T1), el «✓ hizo» del Tutor (#23 T4) y el ocultamiento por vigencia (#24)—: *la unidad verifica la pieza y lo que falla es el cable*. Otras desviaciones: un `GET .../valores-en-monedas` que la Parte C no lista pero la Parte F necesita (el participante tiene que ver el precio **antes** de completar, y el GET del Tutor no sirve) que **devuelve `[]` en DIRECTO**, así «no se muestra en DIRECTO» cae por construcción; y `CONDUCTA_MALA_NO_RINDE` resuelto con una llamada extra **solo en el camino de error**. UI mostrada a José antes de scaffoldear: eligió **fila compacta** y **máximo teórico por semana** para el aviso de calibración (`Σ monedas × repeticiones × sesiones`, con el bono del jefe adentro porque es un techo), cuya aritmética vive en `core/calibracion-monedas.ts` y se testea sin montar Angular. 357/357 activity + 63/63 scoring (+6) + 206/206 rewards (+38) + 159/159 app-web (+10), lint y build verdes, **migración aplicada contra Postgres real y sin drift**, y **suite E2E completa 73/73 en dos corridas seguidas con `E2E_UI=1`** (las 18 de navegador incluidas). Nota de herramienta: **Prisma 7.8 renombró los flags de `migrate diff`** a `--from-config-datasource` / `--to-schema`. Queda anotado que `MonedasPorAccion` **todavía no tiene consumidor** (Notification y Audit no lo escuchan) — la misma deuda que `MonedasAcreditadas` dejó el #22. **#30 alcance total del asistente — EN CURSO, tandas 1 a 7 de 9 (2026-08-05)**: primer ítem que **extiende otro de esta misma fase** y el primero que **corrige defectos de algo entregado en la Fase 14**. El #29 dejó ocho lecturas y cuatro propuestas, o sea un asistente que **ve el grupo entero y puede tocar un cuarto de él**; el ítem lo lleva a 12 lecturas y 15 propuestas. Pero la **tanda 1 no agrega ni una capacidad**: arregla los dos defectos y escribe las dos reglas que los hacen irrepetibles, que es lo que la Parte F llama *«la tanda que hay que hacer aunque el resto se posponga»*. **Defecto 1**: `proponer_precios_tienda` pedía un `productoId` y **ninguna lectura devolvía uno** (el precio vive en `ProductoTienda`, no en `Recompensa`), así que el modelo solo podía inventarlo y la propuesta moría al aplicar. Se cierra con `listar_tienda` (endpoint interno nuevo, tercero de rewards) **y con la regla**: `uuidDe(qué, ...origen)` exige declarar de qué lectura sale cada id, con `NombreHerramientaLectura` derivado de la tupla literal — **un origen inventado no compila** (verificado: `TS2345`). El segundo test es el que de verdad cierra la puerta: **ninguna propiedad puede hablar de un uuid sin estar declarada como tal**, o sea que escribirla a mano salteando `uuidDe` también se pone en rojo (verificado rompiéndolo). **Defecto 2**: la Parte E del #29 promete que el id de organización no sale hacia OpenAI y **salía**, en las cuatro lecturas que devolvían el DTO crudo; las dos que se armaban a mano campo por campo nunca lo tuvieron, y esa es toda la diferencia. Ahora **las nueve moldean su respuesta campo por campo**, con test sobre la **salida real** (ninguna clave `/organizacionId|grupoId|tenant/`, ningún `@`), y **los mocks devuelven los DTOs con tenant adentro a propósito**: con fixtures limpios el test pasaría sin verificar nada, que es exactamente cómo el defecto sobrevivió a la tanda 3. Moldear encontró **tres cosas que cuatro `delete` no habrían tocado**: las etiquetas anidadas de una recompensa son otro DTO con tenant, **`resumen_cumplimiento` era el quinto caso** (la spec dice cuatro porque contó las que devuelven entidades) y **el fallback del servicio caído filtraba el grupo por la puerta del error**. Además, primera instancia de la decisión 2 (el `productoId` se valida contra la tienda real antes de persistir, y el error le dice al modelo qué llamar) y `aJsonSchema()`, que saca los metadatos antes de que el catálogo viaje — no porque el proveedor los rechace, sino para que lo que sale no dependa de que un tercero los ignore. **Costo medido (criterio 12)**: 12→13 herramientas, 16.591→17.267 caracteres, **~+169 tokens (+4,1%)**, con el bloque entrando por caché. 162/162 ai-service (+14) + 206/206 rewards, lint 19/19 y build verdes, **sin migraciones**, y el endpoint nuevo **verificado contra Postgres real** (200 con secreto / 401 sin él, el producto BOLSA con su bolsa y sus dos ítems, aislamiento con tres grupos y ni una clave de tenant en la respuesta real). Pendiente declarado: **la conversación real contra OpenAI llamando a `listar_tienda`** — la clase de cable que esta fase viene encontrando rota siete veces, así que no cuenta como verificado hasta correrlo. **Tanda 2 (2026-08-05)**: los contratos en `shared-types` y sus `implements`, la que la Parte F pide aislada porque **puede romper builds ajenos y no cambia ningún comportamiento**. Doce contratos nuevos (conductas, umbrales, configuración de scoring, recompensas, etiquetas, bolsa y producto-crear). **El relevamiento corrigió a la spec**: los nueve contratos que ella da por *«ya existen y se reusan tal cual»* existen, pero **ninguna de sus clases los `implements`** —los siete de identity y el de turnos nacieron como interfaces para el frontend—, así que la garantía del ítem no valía para ninguno; quedaron enganchados acá y el alcance real fue **8 DTOs de 4 servicios**, no «siete de tres». **EL HALLAZGO DE LA TANDA**: `implements` **no detecta un campo OPCIONAL renombrado** (por tipado estructural la clase le sigue siendo asignable), o sea el **mismo agujero que el #29 encontró del otro lado del cable** con `z.ZodType<Contrato>`; se cierra igual, comparando claves — `contratos.ts` con `Exhaustivo`/`ClavesNoCubiertas` y **20 líneas de cobertura**, una por clase. Van como dos tipos y no como uno que envuelva a los dos porque **`extends never` no se puede verificar sobre genéricos**: se intentó de la forma obvia y el compilador lo rechazó en los seis servicios a la vez. Que esto apareciera en la tanda 2 y no en la 4 es exactamente para lo que la Parte F la pedía aislada. **Verificado rompiéndolo**: renombrar un opcional en el contrato deja el build en rojo nombrando la clave (`TS2344`), y sin la cobertura pasaba en verde. Suite completa del workspace verde **sin un solo test tocado** (activity 357, rewards 206, app-web 205, session 74, scoring 63, gateway 49, identity 48, notification 22, ai 162), lint 19/19, build de los 6 proyectos afectados, **sin migraciones**. Fuera de spec: `docs/architecture/shared-types.md` no listaba **ninguno** de los contratos de request del #29 y se completó. **Tanda 3 (2026-08-05)**: las tres lecturas restantes —`listar_etiquetas`, `listar_turnos` y `configuracion_del_grupo`— con **cinco endpoints internos nuevos** en tres servicios. El catálogo de lectura queda en **doce**, el número final del ítem, y los dos tests estructurales del #29 las cubrieron sin tocarles una línea. `configuracion_del_grupo` es la única que **compone tres servicios en paralelo en una sola respuesta**, por el mismo criterio que `listar_participantes`: partirla costaría dos vueltas más del loop, o sea dos llamadas al proveedor pagadas para contestar una cosa. Trae lo que hace que otros campos signifiquen algo —`planDelDiaActivo`, `puntosIniciales` y sobre todo el **modo de recompensas**, que evita el error más caro: proponer precios en un grupo `DIRECTO` es proponer sobre una tienda que nadie ve—. Un servicio que no contesta **deja su parte en `null` y no en un default**: decir «DIRECTO» sin saberlo haría que el asistente descarte la tienda de un grupo que sí la usa. Los defaults (`RESTRICTIVO`, 0, `DIRECTO`) **no se repiten en los endpoints**: delegan en los services que ya los resuelven —`ConfiguracionService.leer` de rewards pasó de privado a público—, porque dos copias de un default se separan sin que nadie lo note; eso obligó a que `InternalModule` importe módulos de negocio, cableado que compila bien y falla al arrancar. Los turnos van **sin nombres** (eso es una llamada a identity que el consumidor ya hace) y **conservando los repetidos**, con un test que lo fija: que la misma persona aparezca dos veces es cómo se le dan más turnos (#21), y un molde que dedupliqué rompería ese ítem sin que nada se queje. 167/167 ai-service (+5) + activity 357 + rewards 206 + scoring 63 sin regresiones, lint 19/19 y build 19/19, **sin migraciones**, y **los cinco endpoints verificados contra el stack real**: 200 con secreto / 401 sin él, el grupo del piloto devolviendo su configuración real (plan del día prendido, modo `LIBRE`, 100 puntos iniciales, `TIENDA` con moneda «perigreses», dos rotaciones en orden), **los defaults contra un grupo inexistente**, aislamiento de etiquetas entre grupos y **ni una clave de tenant en las cinco respuestas reales**. Costo del catálogo actualizado: 16 herramientas, ~4.734 tokens (**+14% sobre el original**), con las tres lecturas costando ~417 entre las tres — son las baratas; las once propuestas que faltan son las caras.**Tanda 4 (2026-08-05)**: la primera de las cuatro familias y la primera del ítem que **agrega capacidades** en vez de arreglar lo que había — `proponer_crear_conductas`, `proponer_editar_conductas` y `proponer_configurar_turnos`, más los dos campos de actividad de la decisión 8 (`repeticionesMaximasSeccion` y `siempreVisible`, este último destrabado por `configuracion_del_grupo` de la tanda 3). Ningún endpoint destino hubo que crearlo, y es la tanda que **estrena los contratos que la 2 dejó sin consumidor**: los tres esquemas Zod van tipados contra el contrato con su cobertura de claves, o sea que ahora renombrar un campo en activity rompe el build de `ai-service` de los dos lados. Tres cosas que decidió escribirla: **el `null` significa lo contrario que en una actividad** —allá borra el campo (#24), acá el contrato no tiene ni un campo anulable, así que solo puede ser «no lo puse»; y como el modelo **no puede omitir una propiedad declarada**, conservarlo habría hecho fallar TODA edición de conducta contra un error irresoluble—; **la secuencia de turnos viaja plana hacia el modelo y como objetos hacia el endpoint**, porque el test estructural del tenant prohíbe una propiedad `usuarioId` y con la forma del contrato la herramienta no compilaba ese test (**anotado para la tanda 7: la familia personas no lo va a poder esquivar**, `jefeUsuarioId` es un campo del contrato); y **solo se replican las reglas que el destino rechaza** — las cuatro de turnos (OBLIGATORIA, INDIVIDUAL, participante del grupo, dentro del destinatario nominal) y ninguna de conductas, verificado leyendo su service entero. Los criterios 3 y 4 quedaron como test: **ninguna operación usa `DELETE`** (tabla que arma una propuesta real de las siete herramientas y **se compara contra el catálogo**, así que una herramienta nueva sin cubrir se pone en rojo) y **ningún esquema acepta `estado`** (la forma en que la decisión 3 se rompería sin que aparezca la palabra DELETE). Dos cosas entraron fuera de la Parte F porque sin ellas la familia no funciona sola: **el frontend de estos tres tipos** (la unión creció y `app-web` dejó de compilar; una tarjeta vacía convierte «Aplicar» en un botón) y **una línea del system prompt** — las rotaciones no entraban en ninguna de las cuatro capacidades declaradas, o sea que el modelo tenía la herramienta y la instrucción de no usarla: *mismo modo de falla de siempre, encontrado antes de que costara una corrida*. **ai-service 185/185 (+18), app-web 207/207 (+2), workspace entero verde sin una regresión, lint y build 19/19**, migración aditiva aplicada contra Postgres real. Costo: 19 herramientas, ~6.443 tokens (**+55% sobre el original**); medidas una por una, las nuevas pesan 1.7-2.0k caracteres contra 5.8k de las de actividades, o sea que **lo que cuesta es la cantidad de campos, no la prosa**. Pendiente explícito: el apply de punta a punta contra activity con un JWT de Tutor es de la tanda 9, no se da por verificado.**Tanda 5 (2026-08-05)**: la familia más grande del ítem — cinco herramientas contra cuatro endpoints de rewards (premios y castigos, bolsas, productos, etiquetas), catálogo en **23 herramientas**. Incluye la **decisión 7**: `proponer_precios_tienda` pasó a `proponer_editar_productos` y dejó de cubrir solo el precio (el mismo PATCH acepta nombre, fuente, mecánica y qué entrega), pero **el valor `PRECIOS_TIENDA` del enum se conserva** porque está persistido en filas existentes — que el nombre y el tipo no coincidan es deliberado y está explicado en los tres lugares donde alguien lo va a mirar. **El chequeo de contrato encontró una propuesta que el endpoint habría rechazado**: compartir `descripcion` entre crear y editar recompensa no compila, porque en el alta es `string | undefined` y en el PATCH es `string | null` (en un alta no hay nada que borrar). Habría dejado pasar un alta con `descripcion: null` que rewards contesta con 400, o sea **una propuesta aplicada que le sale fila roja al Tutor** — exactamente lo que la decisión 11 existe para hacer imposible, y la clase de detalle que no se encuentra leyendo. Cinco reglas del destino replicadas, todas leídas en su código: la zona obligatoria en modo DIRECTO (**y si el servicio no contesta el modo no se inventa**, mismo criterio que la tanda 3), las dos puertas contra el castigo comprable (#26 decisión 20), las referencias excluyentes ITEM/BOLSA **validadas sobre el estado fusionado** como hace el endpoint, la bolsa vacía o archivada, y el tope de 5 etiquetas. **Criterio de aceptación 6 cumplido**: un producto que apunta a una bolsa de la misma propuesta se rechaza con un error que dice **qué hacer** —«proponé primero las bolsas y después, en otra propuesta, los productos que las venden»— y el mensaje cambia según si la propuesta está creando bolsas; es la lección de `invariantes.ts`: un error que solo describe el problema empuja al modelo a inventar un valor. Y el único array vacío del ítem que **significa algo**: `etiquetaIds: []` es «sacale todas» (el PUT reemplaza la lista completa, #26), así que ese campo no pasa por `limpiarVacios` y la tarjeta lo dice con todas las letras. Frontend otra vez en la tanda —la unión creció y `app-web` deja de compilar—: las dos altas de tienda se distinguen **por la ruta**, que es el dato que ya viaja, y el contexto pasó de 6 a 11 lecturas. Fuera de spec: `core/herramientas-ia.ts` (el rastro que el Tutor ve mientras el modelo trabaja) tenía solo las 12 del #29 y las cuatro lecturas nuevas salían con su nombre técnico en pantalla; quedaron las 23. Y el system prompt otra vez, **segunda tanda seguida**: la capacidad 4 no cubría premios, castigos, etiquetas ni bolsas. **ai-service 204/204 (+19), app-web 210/210 (+3), workspace verde sin regresiones, lint y build 19/19**, migración aditiva aplicada contra Postgres real. Costo: 23 herramientas, ~8.792 tokens (**+112% sobre el original**); las cinco nuevas pesan 1.8-2.9k caracteres, todas por debajo de las de actividades (5.8k). **T6 (familia escala)**: una sola herramienta, `proponer_umbrales_zona`, y la más distinta del ítem — la única que se valida como **conjunto** (los rangos cubren la recta sin huecos ni solapes, órdenes corridos desde 1 y exactamente una zona sin techo, todo sobre el estado **resultante**) y la única cuyo efecto **cambia el pasado**. **El hallazgo: que el estado final cierre NO alcanza.** scoring valida la escala completa en CADA escritura y aplicar es un `for`, así que una propuesta impecable puede fallar en la operación 1 porque el estado **intermedio** deja un hueco — el caso más común de todos lo tiene (agregar una zona arriba: si el `for` empieza por el alta, quedan dos zonas sin techo y rebota). Por eso el armador **le busca un orden** donde todos los pasos intermedios también cierren, midiéndolos con la regla del *endpoint* y no con la propia; y las propuestas que **no tienen ningún orden posible** (correr dos límites a la vez) no se guardan, con un error que explica qué sí se puede proponer. Límite del producto, escrito: **el asistente amplía la escala, no la re-escala**. Segundo hallazgo: **`puntosMax` es el único campo del ítem donde `null` es un valor** («sin techo» = la zona más alta) y el modelo no puede omitir propiedades, así que un null de «no lo toqué» llega idéntico a uno de «sacale el techo»; se resolvió **sacándole la ambigüedad al pedido** —la zona se propone entera, los cinco campos siempre— lo que además arregla que el PATCH de scoring conserve el techo viejo si el campo no viene. Una regla **más estricta que el endpoint** y a propósito (criterio 5): exactamente una zona sin techo, porque una escala con techo arriba deja sin zona a quien lo pase. El aviso de la decisión 6 se calcula en el **backend** y no en el frontend (desviación de la Parte D): `resumen_puntajes` es lectura interna y la pantalla tendría que encadenar `session-service` para lo mismo, y calculado al armar **queda guardado** con la propuesta — viaja en un `PropuestaIaDto.aviso` nuevo, dentro del `snapshot` que ya existía. Un conteo que no se puede calcular **se dice, no se inventa**. Frontend en la tanda otra vez (la unión creció) y **system prompt por tercera vez seguida**: la escala no entraba en ninguna capacidad, o sea herramienta con instrucción de no usarla. **ai-service 232/232 (+28, con `escala.spec.ts` nuevo), app-web 215/215 (+5), workspace verde, lint 19/19, build 18/18**, migración aditiva aplicada y verificada contra Postgres real (12 valores del enum). Costo: 24 herramientas, ~9.613 tokens (**+132%**); la primera versión de la herramienta pesaba 4.111 caracteres —los cinco campos entran dos veces, así que cada palabra de más se paga doble— y quedó en 3.282 sin sacar ni una regla. **T7 (familia personas, la última)**: `proponer_roles_grupo` y `proponer_equipos`, con lo que el catálogo **cierra en 26 herramientas — 12 lecturas y 14 propuestas**, contra las 8 y 4 del #29. **Se resolvió el choque que la T4 dejó anunciado**: el test estructural del tenant prohíbe `usuarioId`… y también `grupo`, así que `rolGrupoId` —un campo del contrato de identity sin nada de tenant— tampoco pasaba. En vez de aflojar la regex, el catálogo usa su **propio vocabulario** (`participanteId`, `rolId`, `equipoId`) y el armador traduce a los nombres del contrato, igual que la T4 con `posiciones`: la regla es una defensa que no depende de que el modelo se porte bien, el costo de mantenerla es una línea de mapeo, y **lo que garantiza que el id sea el correcto no es su nombre** sino que se declara con origen (decisión 1) y se valida contra el grupo real (decisión 2) — con test propio sobre eso. La regla del destino que más va a rechazar: **una persona está en un solo equipo**, y no alcanza con que la propuesta no se contradiga a sí misma (hay que mirar dónde está cada uno hoy **y a quién ya ubicó la propia propuesta**); el error dice qué hacer, porque **mudar a alguien de equipo es imposible para el asistente** —quitar es un `DELETE`, decisión 3— y lo único que puede hacer es explicarlo. Sumar a alguien y **ascenderlo a jefe en el mismo cambio** sí vale: el `POST` del miembro va antes que el del jefe, con test que fija el orden. `rolGrupoId: null` (dejar sin rol) **sí se expone** y no contradice la decisión 3: no destruye nada y es la única forma de decirlo en un endpoint que fija un valor — lo que sí queda afuera es `estado`, porque archivar un rol **desasigna a todos**. Con esta tanda **los ocho contratos de identity que la T2 dejó sin consumidor lo tienen**. Frontend por cuarta vez en la tanda: roles y participantes pasan a viajar enteros en el contexto (una propuesta los edita, hace falta el «antes» de cada campo) y `rolGrupoId: null` se lee «sin rol», hermano exacto del «sin techo» de la T6. **ai-service 250/250 (+18), app-web 220/220 (+5), workspace verde, lint 19/19, build 18/18**, migración aditiva aplicada contra Postgres real (enum en 14 valores). Costo final: 26 herramientas, **~10.962 tokens (+164%)**, justo donde la T4 lo había estimado; las dos nuevas pesan 2.6k y 2.8k, en el rango de economía. **T8 (frontend, 2026-08-06)**: la tanda más chica, porque las cuatro familias se llevaron el grueso de la Parte D — quedaban **las cuatro entradas de contexto** («Pedirle ayuda a la IA» en Conductas, Recompensas, Tienda y Zonas) y repasar el conjunto. Antes de sumarlas entró un **componente único** (`entrada-asistente`) con las dos variantes que ya existían —tarjeta con la pantalla vacía, enlace con la pantalla llena— y las dos del #29 migradas a él: seis copias del mismo `@if (asistenteDisponible())` era la garantía de que la sexta se olvidara de preguntar, y **un atajo a una pantalla que no funciona es peor que no tener el atajo**. Con eso el **criterio 14 pasa a cumplirse por construcción y con test**: la entrada no hace ninguna llamada propia, lee el cache que el shell cargó una vez por sesión, y con `ai-service` caído ese cache queda en `null` y no se dibuja nada — las seis pantallas enteras y sin atajo, sin estado intermedio esperando una respuesta que no llega. Dos condiciones que salieron de mirar la pantalla y no la spec, las dos en el catálogo de recompensas: **con un filtro de etiqueta puesto el catálogo no está vacío** (le sobra un chip, no le faltan ítems) y **en modo `DIRECTO` sin ninguna zona la propuesta muere al aplicar** porque `umbralZonaId` es obligatorio, así que el atajo usa la misma condición que ya deshabilita «Nuevo». El catálogo lleva además **dos preguntas según el modo** —un premio cuelga de una zona en `DIRECTO` y de ninguna en `TIENDA`— porque el primer mensaje también lo lee el Tutor. Y **Zonas es la única pantalla con las dos variantes**: en las otras tres el asistente sirve para empezar y con contenido cargado sería ruido; en Zonas, una vez armada la escala, la pregunta que queda es «¿sigue sirviendo?», la única del ítem que se contesta mirando los puntajes reales — y ahí paga el aviso de la decisión 6 que la T6 dejó en el backend, el conteo de cuántos cambian de zona, que desde esa pantalla no se puede calcular a mano. La revisión de precios, que sería el equivalente para Tienda, **ya existe y no se duplicó**: el enlace de Rendimientos la cubre y desde la T1 puede terminar en `proponer_editar_productos` de verdad. Repaso de la Parte D, sin cambios necesarios: los tres mapas, un `case` por cada uno de los 14 `TipoPropuesta`, las doce lecturas del contexto (roles, participantes y umbrales enteros; equipos, bolsas y etiquetas como mapas, que es lo correcto porque solo se los referencia por id), el aviso genérico en la tarjeta y las 26 herramientas con nombre legible en `herramientas-ia.ts`. **app-web 224/224 (+4), lint limpio y build de producción ok**; backend sin tocar, catálogo sigue en 26 herramientas / ~10.962 tokens. Falta solo la T9 (E2E). **T9 (E2E, 2026-08-06) — con esto el ítem 30 CIERRA, 9 de 9 tandas**: ocho tests nuevos en `asistente-ia.e2e.ts` (17 → **25**) más una suite de navegador propia, `asistente-entradas.e2e.ts`. Mismo criterio que el #29: el proveedor está stubbeado, así que **no se testea que el modelo proponga cosas buenas** — se testean los **cables**, que es lo que este ítem viene encontrando roto: que el id que valida el armador sea el que devuelve la lectura, que lo que sale hacia el proveedor no lleve el tenant, y que el orden en que se guardan las operaciones sea el orden en que se pueden aplicar. **El primer hallazgo fue la propia suite**: afirmaba `toHaveLength(12)` sobre el catálogo de herramientas y ese número dejó de ser cierto en la T1, o sea que la afirmación estuvo **falsa siete tandas** — la E2E no corre sola (necesita el stack a mano, no está en el `nx run-many` de cierre de tanda) y el job de CI no la vio nunca porque la rama va 16 commits adelante del remoto. Es el modo de falla del ítem un piso más arriba: *la unidad verifica la pieza, la E2E verifica el cable, y nada verifica que la E2E se corra*. Los tests, uno por criterio: aislamiento de las **cuatro lecturas nuevas** (7); **las doce lecturas** sin `organizacionId`/`grupoId`/`tenant`/email, afirmado sobre la **respuesta real** y no sobre el tipo, más que las doce corrieron **sin error** —un error también es una salida «limpia»— (11); **ocho ids reales de la otra organización**, uno por familia, ninguno crea `Propuesta` y el error nombra el campo (2); la escala con un hueco que no se guarda y dice **dónde** está el hueco (5); el producto que apunta a una bolsa de su misma propuesta, rechazado con el error que **explica el orden** (6); tres conductas con la segunda rota → 2 creadas y `APLICADA_PARCIAL` (9); y el de la escala, que verifica el orden **de las dos maneras** —que la propuesta traiga `PATCH` antes que `POST` y que al revés falle de verdad contra scoring (400)— y después comprueba que el participante que estaba en Dorado quedó en Platino **sin sumar un punto**, o sea el número del aviso contrastado contra el estado y no contra sí mismo (10 + pendiente 15). En navegador: sin el asistente usable las pantallas se dibujan enteras y sin atajo (14), cada pantalla ofrece la entrada que le toca, y el atajo llega al asistente **con la pregunta ya escrita y mandada** (pendiente 20). Hallazgo operativo: el test de los ocho ids se come el presupuesto del rate limiter del Gateway y el siguiente se comió la ventana entera —no es flakiness, la salida es `test.slow()`, que es el patrón que `support/api.ts` ya documenta—. **Verificación: 25/25 propia; suite completa con los dos frontends servidos 101 passed / 1 skipped, DOS corridas seguidas; sin UI 80 passed / 21 skipped, también dos; `ai-service` 250/250 sin tocar una línea de producción.** Y se verificó por primera vez lo que las T4 a T7 dejaron anotado: el **apply de punta a punta con JWT de Tutor** contra activity, rewards, scoring e identity. Deuda anotada: la E2E no corre sola (correrla al cerrar cada ítem, no cada tanda) y las suites de navegador siguen gateadas por `E2E_UI=1`, fuera de CI.  **#31 alcance operativo del asistente (borrar, ajustar, anotar) — EN CURSO, 4 de 9 tandas (2026-08-06)**: pedido de José —*«que la IA pudiera borrar ahora, también si el usuario confirma, además que pueda modificar los puntos de algún participante o sus monedas, o anotar qué hizo o no hizo»*—, que es **exactamente lo que el #29 y el #30 dejaron fuera de alcance a propósito**, así que va como ítem propio con su spec (`fase-14-31-alcance-operativo-del-asistente.md`) y sin editar ninguna anterior. Cuatro decisiones cerradas con José antes de tocar código. **Encontró un hueco que no era de la IA**: no existía ajuste manual de puntos —`corregir` exige el id de un asiento previo—, o sea que **el Tutor tampoco podía sumar 10 puntos a mano desde la Fase 7**, mientras que para monedas lo tenía desde el #22; por eso la tanda 1 construye el endpoint **con su pantalla** y sin una línea de IA (fila nueva `AJUSTE_MANUAL`, `origenId` nullable, motivo obligatorio, cae en la sesión abierta o 409, sin piso en 0 porque un puntaje negativo es una zona legítima). El precedente que deja el ítem está en la tanda 2: la decisión 3 del #30 —*ninguna operación usa `DELETE`*— **no se borra, se invierte** en una lista blanca (`ARCHIVAR_CATALOGO`, `QUITAR_MARCAS`, `UMBRALES_ZONA`), que es una propiedad más fuerte que quedarse sin test — *una capacidad que se amplía deja atrás una regla más chica, no ninguna*. La tarjeta destructiva se decide **por las operaciones y no por el tipo** (una propuesta de umbrales con una zona borrada adentro es destructiva): encabezado rojo, **sin «Aplicar todo»**, nada tildado de entrada y el cartel que dice qué se pierde *y qué no* —casi todos los `DELETE` son soft, y el Tutor que cree que archivar borra los puntos no aprieta nunca—. Tanda 3: `estado_de_hoy` es a las marcas lo que `listar_tienda` fue al `productoId` —**ninguna lectura devolvía un `registroId`**— y trae resueltas las reglas de «se puede marcar» desde donde vive el endpoint que las hace cumplir, más `listar_billeteras` para que un descuento no proponga dejar el saldo bajo 0. Tanda 4: `proponer_archivar` y `proponer_quitar_marcas`, con etiquetas escritas una por tipo (archivar una bolsa deja mudos a los productos que la venden; archivar una etiqueta no rompe nada) y validación de que el id sea de la entidad que dice ser. **Verde: ai-service 263, app-web 229, activity 357, rewards 206, scoring 71 — test, lint y build.** **Faltan las tandas 5 a 9** (ajustes manuales, anotar, borrar zonas dentro de la escala, aviso de consentimiento v2 con re-aceptación, E2E) y queda anotado que **las dos migraciones aditivas no se corrieron todavía contra Postgres** y que el endpoint de ajuste de puntos solo se probó con la BD en memoria. |

Estados posibles: `PENDIENTE` · `EN_PROGRESO` · `COMPLETADA` · `COMPLETADA_CON_DESVIACIONES` (completada pero con algo documentado que se apartó del plan o quedó como deuda técnica — ver su archivo en esta carpeta).

## Por qué está separado de `docs/phases/`

Mismo principio que la regla de puntaje del proyecto: lo ya escrito no se edita silenciosamente. `docs/phases/` es la decisión tomada de antemano; `docs/progreso/` es el hecho consumado. Si en algún momento la realidad obliga a cambiar la spec misma (no solo registrar una desviación puntual), eso se hace a mano, a propósito, y se nota en el historial de git — no se pisa silenciosamente vía una sesión de Claude Code ejecutando código.
